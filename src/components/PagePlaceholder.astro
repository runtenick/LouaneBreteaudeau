---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<section class="flex min-h-[calc(100vh-7rem)] items-center justify-center px-6 text-center">
	<h1 class="text-sm font-light uppercase tracking-[0.28em] text-neutral-400 sm:text-base">
		{title}
	</h1>
</section>
